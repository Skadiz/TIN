<!DOCTYPE html>

<html lang="pl-PL">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="../../css/style.css">
    <script type="application/javascript" src="../../js/validationRoomForm.js"></script>
    <script type="application/javascript" src="../../js/validationCommon.js"></script>
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>Hotel "TIN2021"</title>
</head>

<body>
<div id="all" >

<div id="header" >
<H1 id="title">Hotel "TIN2021"</H1>
<img  src="../../images/logo.png"  class="logo" alt="logo">
</div>
<div id="menu" >
<a href="../"><div id="link" style="margin-left: 50px; ">Strona Główna</div></a>
<a href="/guests/"><div id="link1" >Gość</div></a>
<a href="/rooms/"><div id="link2" style="background-color:#5c8ebe;">Pokój</div></a>
<a href="/reservation/"><div id="link3" >Rezerwacja</div></a>
</div>
<div id="wrapper">
<br>
	
<h1>Nowy Pokój</h1><hr>
<!-- onsubmit="return validateForm();"-->
<form class="form" novalidate  method="post"  action="<%= formAction %>">
<div class="group">
<label >Numer:<abbr title="required" aria-label="required">*</abbr></label>
<input type="number" name="roomNumber" id="roomNumber" value="<%= room.roomNumber %>" placeholder="100-500" class="<%= validationErrors.find(e => e.path.includes('roomNumber')) ? 'error-input' : '' %>" />
<span id="errorNumber" class="errors-text"> <% if (validationErrors.find(e => e.path.includes('roomNumber'))) { %>
        <%= validationErrors.find(e => e.path.includes('roomNumber')).message %>
    <% } %></span>

<label  >Typ:<abbr title="required" aria-label="required">*</abbr></label>
<input type="text" name="type" id="type" value="<%= room.type %>" placeholder="2-60 znaków" class="<%= validationErrors.find(e => e.path.includes('type')) ? 'error-input' : '' %>"/>
<span id="errorType" class="errors-text" ><% if (validationErrors.find(e => e.path.includes('type'))) { %>
        <%= validationErrors.find(e => e.path.includes('type')).message %>
    <% } %></span>
<label  >Cena(zł):<abbr title="required" aria-label="required">*</abbr></label>
<input type="number" name="price" value="<%= room.price%>" id="price" class="<%= validationErrors.find(e => e.path.includes('price')) ? 'error-input' : '' %>">
<span id="errorPrice" class="errors-text"><% if (validationErrors.find(e => e.path.includes('price'))) { %>
        <%= validationErrors.find(e => e.path.includes('price')).message %>
    <% } %></span>
<label  >Ilość miejsc:<abbr title="required" aria-label="required">*</abbr></label>
<input type="number"  name="numberOfPlaces" value="<%= room.numberOfPlaces %>" id="numberOfPlaces" placeholder="1-5" class="<%= validationErrors.find(e => e.path.includes('numberOfPlaces')) ? 'error-input' : '' %>" />
<span id="errorPlaces" class="errors-text"><% if (validationErrors.find(e => e.path.includes('numberOfPlaces'))) { %>
        <%= validationErrors.find(e => e.path.includes('numberOfPlaces')).message %>
    <% } %></span>
</div>
<div class="buttons">
<p id="errorsSummary" class="errors-text"></p>
<br><input type="submit" value="Dodaj" class="button-submit"  />
    <a href="/rooms/" class="button-cancel">Anuluj</a>    
</div>
</form>


</div>
<br>
<%- include('../fragments/footer.ejs') %>